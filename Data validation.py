#Data validation
def lengthcheck(word, length, option):
    if option ==1:
        if len(word)==length:
           return True
    else:
    return False

# option 2 checking if the word has length not shorter than lentgh

     elif option == 2:


if len(word) >= length:
    return True else:
    return False

             if len(word) >= length: return True else: return False

# option 3 checking if the word has length not longer than length

elif option == 3: if len(word)<= length: return True else: return False